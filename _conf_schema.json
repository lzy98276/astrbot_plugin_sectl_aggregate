{
  "api_base_url": {
    "description": "回声洞 API 基础地址",
    "type": "string",
    "hint": "Appwrite 实例的 URL 地址",
    "default": "https://appwrite.sectl.cn"
  },
  "api_token": {
    "description": "API Token（必填：用于所有 API 认证，含内部接口）",
    "type": "text",
    "hint": "Appwrite JWT 或管理后台生成的 API Key，用于 /api/echo-cave/internal 和绑定等所有端点",
    "default": ""
  },
  "hub_token": {
    "description": "Hub Token（可选：用于 Hub 内容中心内部接口，不填时回退到 api_token）",
    "type": "text",
    "hint": "x-hub-token 认证头对应的 Token",
    "default": ""
  },
  "request_timeout": {
    "description": "请求超时时间（秒）",
    "type": "float",
    "hint": "HTTP 请求的最大等待时间",
    "default": 10.0
  },
  "retry_count": {
    "description": "重试次数",
    "type": "int",
    "hint": "API 请求失败时的重试次数",
    "default": 2
  }
}
