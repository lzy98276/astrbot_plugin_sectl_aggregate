{
  "api_base_url": {
    "description": "回声洞 API 基础地址",
    "type": "string",
    "hint": "Appwrite 实例的 URL 地址",
    "default": "https://appwrite.sectl.cn"
  },
  "api_token": {
    "description": "API 认证 Token（必填：用于 Appwrite JWT / API Key 认证）",
    "type": "text",
    "hint": "Appwrite JWT 或管理后台生成的 API Key，用于 /api/qq-binding/status 等需认证端点",
    "default": ""
  },
  "internal_token": {
    "description": "内部投稿 Token（必填：用于回声洞投稿认证）",
    "type": "text",
    "hint": "用于 /api/echo-cave/internal 接口的 x-echo-cave-token，未配置时投稿不可用",
    "default": ""
  },
  "request_timeout": {
    "description": "请求超时时间（秒）",
    "type": "float",
    "hint": "HTTP 请求的最大等待时间",
    "default": 10.0
  },
  "retry_count": {
    "description": "重试次数",
    "type": "int",
    "hint": "API 请求失败时的重试次数",
    "default": 2
  }
}
